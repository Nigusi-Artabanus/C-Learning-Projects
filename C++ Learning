#include <iostream>
#include <string>
using namespace std;
void greet(string name) {
  cout << "Hi " << name << "\n";
}

int main(){
  string uName;
  cout << "What's your name? ";
  cin >> uName;
  greet(uName);
  return 0;
}
