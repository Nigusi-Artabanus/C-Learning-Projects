#include <iostream>
#include <string>
using namespace std;
void greet(string name) {
  cout << "Hi " << name;
}

int main(){
  cout << "What's your name? ";
  cin >> uName;
  greet(uName);
  return 0;
}
